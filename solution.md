# xv6 lab solutions — 总结

本文档总结了四个 user-level 程序的任务要求与实现要点（pingpong, primes, find, xargs），并说明了实现细节与可能的陷阱。

---

## 1) pingpong (easy)   

### 要求
- 使用 xv6 系统调用在父子两个进程间“乒乓”传递一个字节。
- 需要两个 pipe：父 -> 子、子 -> 父。
- 父进程先发送一个字节；子进程收到后输出 `<pid>: received ping`，再把字节写回父进程并退出；父收到字节后输出 `<pid>: received pong` 并退出。

### 实现要点（user/pingpong.c）
- 创建两条管道：`fd_p2c`（父->子）和 `fd_c2p`（子->父）。
- 调用 `fork()` 创建子进程。
- 每个进程关闭不需要的端点（read/write）以避免死锁和资源泄漏。
- 父进程写入 `fd_p2c[WR]`，然后从 `fd_c2p[RD]` 读取；子进程相反。
- 使用 `getpid()` 来打印进程 id；在读写失败时打印错误并返回非零状态码。

### 注意点
- 必须关闭不需要的 pipe 端，否则会使读端永远认为写端仍存在，从而阻塞或导致资源用尽。

---

## 2) primes (moderate/hard) 

### 要求
- 使用管道和 fork 实现并发素数筛（Doug McIlroy 的想法）：每发现一个素数都产生一个新的进程和一个新的管道，将不被素数整除的数转发到下一级。
- 第一个进程生成 2..280 的数字并写入第一个管道；链式过程应在写入端关闭后正确终止。

### 实现要点（user/primes.c）
- 使用 `int`（4 字节）直接在管道中读写数字，避免使用文本格式。
- `main` 创建第一个管道并 fork：父端写入 2..280，然后关闭写端并 `wait(0)`；子进程调用 `prime(p[RD])`。
- `prime(int fd_rd)`：从 `fd_rd` 读取第一个整数 n，打印 `prime n`；为下一层创建管道 `p`，fork 出子进程让子去处理 `p[RD]`（递归），父进程从 `fd_rd` 读取并把不能被 n 整除的数写入 `p[WR]`。最后父关闭 fd 并 `wait(0)` 等待其子完成。

### 关键点
- 每个进程必须关闭它不需要的管道端口（读/写）——否则会耗尽文件描述符或造成阻塞。
- 当上游写端关闭时，下游 `read` 返回 0，作为递归结束条件。
- 由于递归和 fork 的混合，确保最终 `wait` 能在所有子进程退出后才返回，主程序才退出。

---

## 3) find (moderate)  

### 要求
- 实现一个简单的 `find`：在目录树中查找名字匹配的文件并打印路径。
- 需要递归进入目录，跳过 `.` 和 `..`，并能够处理文件与目录两种情况。

### 实现要点（user/find.c）
- 使用 `open` 打开目录或文件并 `fstat` 获取类型（文件或目录）。
- 如果是文件，使用 `fmtname` 提取末尾名称并与目标名 `strcmp` 比较；匹配则打印完整路径。
- 如果是目录，构建缓冲 `buf`（`path/`），读取 `struct dirent` 条目（`read(fd, &de, sizeof(de))`）并递归调用 `find`。忽略 `de.inum==0`、"." 和 ".."。

### 注意点
- 路径缓冲区大小要检查，避免 overflow（本实现有检查并报错）。
- `fmtname` 用于取得路径的最后一段名称（类似 basename）。

---

## 4) xargs (moderate)  

### 要求
- 实现一个简化版的 UNIX `xargs`：接收命令与固定参数，从标准输入逐行读取内容，把行拆成单词并作为后续参数执行命令。
- 每读取一行就 fork/exec 一个子进程来运行该命令，父进程等待子进程结束。

### 实现要点（user/xargs.c）
- 限制：`MAXLINE = 512`、`MAXARG = 32`，将命令及固定参数先放进 `argv_new` 数组前部。
- 逐字符读取标准输入（`read(0, &c, 1)`），遇到 `\n` 结束一行。
- 将每行基于空格拆分成单词，附加到 `argv_new` 中，然后 fork -> exec -> wait。
- 文件末尾未换行的最后一行也要处理。

### 注意点
- 必须确保总参数数目不超过 `MAXARG`，以及每行长度限制在 `MAXLINE`。
- 空格连续出现时应跳过重复空格，避免生成空参数项。


