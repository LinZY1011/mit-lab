# Copy-On-Write (COW) Fork Implementation in xv6

## 1. 概述
本次实验实现了 Copy-On-Write (COW) fork。在标准的 fork 中，父进程的内存会被完整复制给子进程，这在内存占用大且 fork 后立即 exec 的场景下非常低效。COW fork 通过共享物理内存页来优化这一过程，只有当进程尝试写入共享页时，才真正进行内存复制。

## 2. 修改内容

### 2.1 `kernel/riscv.h`
- 定义了 `PTE_COW` 标志位（使用 RSW 位，第 8 位），用于标记页面为 COW 页面。
  ```c
  #define PTE_COW (1L << 8)
  ```

### 2.2 `kernel/kalloc.c`
- 引入了物理页面的引用计数机制。
- 添加了 `ref_counts` 数组和 `ref_lock` 自旋锁。
- 修改了 `kinit` 初始化锁。
- 修改了 `freerange`，在释放页面前将其引用计数初始化为 1。
- 修改了 `kalloc`，分配页面时将引用计数设为 1。
- 修改了 `kfree`，释放页面时递减引用计数，只有当计数为 0 时才真正释放物理页。
- 添加了 `kref_inc` 函数，用于增加页面的引用计数。

### 2.3 `kernel/vm.c`
- **`uvmcopy`**: 修改为不再分配新内存和复制内容。
  - 将父进程的 PTE 中的 `PTE_W` 清除，并设置 `PTE_COW`。
  - 将物理页映射到子进程，同样设置 `PTE_COW` 且无 `PTE_W`。
  - 调用 `kref_inc` 增加物理页的引用计数。
  - 调用 `sfence_vma()` 刷新 TLB。
- **`copyout`**: 修改为在向用户空间写入数据时，如果遇到 COW 页面，触发 COW 处理（分配新页）。
- **`cowalloc`**: 新增辅助函数。
  - 检查页面是否为 COW 页面。
  - 分配新物理页，复制原页面内容。
  - 更新 PTE 指向新页面，设置 `PTE_W`，清除 `PTE_COW`。
  - 递减原物理页的引用计数。

### 2.4 `kernel/trap.c`
- **`usertrap`**: 处理页面错误（scause 15, Store/AMO page fault）。
  - 当发生写缺页异常时，检查是否为 COW 页面。
  - 调用 `cowalloc` 进行页面复制和重新映射。
  - 如果内存不足（`cowalloc` 失败），杀死进程。

### 2.5 `kernel/defs.h`
- 添加了 `kref_inc` 和 `cowalloc` 的函数声明。

## 3. 涉及的操作系统原理

### 3.1 虚拟内存与分页 (Virtual Memory & Paging)
COW 依赖于分页机制。通过操作页表项（PTE）的权限位（如 `PTE_W`），内核可以控制进程对内存的访问。当进程尝试写入只读页面时，CPU 触发缺页异常，内核捕获该异常并进行处理。

### 3.2 缺页异常处理 (Page Fault Handling)
这是 COW 的核心。内核利用硬件的异常机制来实现“懒惰”策略。只有在真正需要写入时（触发 Page Fault），才进行物理内存的分配和复制。这体现了操作系统中常见的“Lazy Allocation”思想。

### 3.3 引用计数 (Reference Counting)
由于多个进程（父进程和子进程）共享同一个物理页，内核必须跟踪有多少个页表指向该物理页。只有当最后一个引用被删除时，物理页才能被回收。这是资源管理中常用的技术。

### 3.4 并发控制 (Concurrency Control)
在多核环境下，多个进程可能同时 fork 或 exit，从而并发访问引用计数数组。因此需要使用锁（spinlock）来保护引用计数的更新，确保数据一致性。

### 3.5 TLB 管理
修改页表后，必须刷新 TLB（Translation Lookaside Buffer），以确保 CPU 使用最新的地址转换信息。在 RISC-V 中使用 `sfence.vma` 指令。

## 4. 详细功能解析

### 4.1 解决的问题
在未修改的 xv6 中，`fork()` 系统调用会把父进程的所有物理内存页完整地**复制**一份给子进程。这带来了两个主要问题：
1.  **性能低下**：如果父进程占用大量内存，复制过程会非常耗时。
2.  **内存浪费**：在 Unix/Linux 系统中，`fork()` 之后通常紧接着会调用 `exec()` 来运行一个新程序。`exec()` 会丢弃刚才复制的所有内存并加载新程序。这意味着 `fork()` 期间辛苦复制的内存数据大部分根本没被用到就被丢弃了。

### 4.2 实现的功能 (Copy-On-Write)
代码实现了**写时复制**优化策略。核心思想是：**推迟物理内存的分配和复制，直到真正需要写入的时候才进行。**

具体功能点如下：

#### A. 优化的 `fork()` (在 `uvmcopy` 中)
- 当父进程 `fork()` 子进程时，不再复制物理内存页。
- 而是让子进程的页表**指向父进程相同的物理页**。
- 将父子进程中这些共享页面的权限都设置为 **只读 (Read-Only)**，并标记一个特殊的 **COW 标志位** (`PTE_COW`)。

#### B. 缺页异常处理 (在 `usertrap` 中)
- 当父进程或子进程尝试**写入**这些共享的只读页面时，CPU 会触发 **页面错误 (Page Fault)** 异常（scause = 15）。
- 内核捕获这个异常，检测到这是一个 COW 页面。
- **按需分配**：内核此时才分配一个新的物理页。
- **复制数据**：将原页面的内容复制到新页面中。
- **更新映射**：修改触发异常进程的页表，使其指向这个新页面，并将权限修改为 **可写 (Writable)**。
- 恢复执行：程序继续运行，就像什么都没发生过一样，但现在它拥有了自己的私有副本。

#### C. 物理页引用计数 (在 `kalloc.c` 中)
- 由于多个进程可能共享同一个物理页，内核必须知道何时可以安全地释放一个物理页。
- 引入了 **引用计数 (Reference Counting)** 机制：
    - `kalloc()` 分配页面时，计数为 1。
    - `fork()` 共享页面时，计数加 1。
    - 进程退出或解除映射 (`munmap`) 时，计数减 1。
    - 只有当引用计数降为 0 时，`kfree()` 才会真正把物理页放回空闲链表。

#### D. 内核写用户内存处理 (在 `copyout` 中)
- 当内核通过 `copyout` 向用户空间写入数据时（例如 `read()` 系统调用），如果目标页面是 COW 页面，也需要像用户态写操作一样触发写时复制，分配新页面，否则内核会写入失败。
